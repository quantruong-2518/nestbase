export enum WEEKLY_RELEASE_DAY {
  MON = 'Monday',
  TUESDAY = 'Tuesday',
  // TODO: need to move on
}

export enum MANGA_STATUS {
  IN_PROGRESS = 0,
  DONE = 1,
  PENDING = 2,
}

export enum READING_TYPE {
  COMIC = 0,
  STORY = 1,
}
